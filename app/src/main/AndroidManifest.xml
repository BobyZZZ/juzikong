<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bb.kanjuzi">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".global.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".mvp.view.MainActivity"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.view.DetailPageActivity" />
        <activity android:name=".mvp.view.SortDetailPageActivity" />
        <activity android:name=".mvp.view.SearchResultActivity"/>
        <activity android:name=".mvp.view.AllLikedActivity"/>
        <activity android:name=".mvp.view.AppThemeActivity"/>
        <activity android:name=".mvp.view.TestActivity"/>
    </application>

</manifest>